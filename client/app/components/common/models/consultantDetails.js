var agence;
(function (agence) {
    var common;
    (function (common) {
        var models;
        (function (models) {
            var ConsultantDetails = (function () {
                function ConsultantDetails() {
                }
                return ConsultantDetails;
            }());
            models.ConsultantDetails = ConsultantDetails;
        })(models = common.models || (common.models = {}));
    })(common = agence.common || (agence.common = {}));
})(agence || (agence = {}));
